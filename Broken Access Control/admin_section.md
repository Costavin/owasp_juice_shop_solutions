# Admin Section

This can be easily solved by looking around in the `Source Page`, and following for words that match `admin`. Then the section is accessible at the following address:

`http://localhost:3000/#/administration`
