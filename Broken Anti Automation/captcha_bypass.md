# CAPTCHA Bypass    -   &#9733; &#9733; &#9733;

Once we intercept a `POST` request for a user feedback, we can send it to the `Intruder` interface in `Burp Suite` and deliver a random payload for at least 20 times.
