# Confidential Document -   &#9733;

When we start digging aroung with `ffuf`, we find out that exists the directory `ftp`. Then if we look around we find out a list of files, which can be accessable if they have extension .md or .pdf. The file `acquisitions.md` looks like quite confidential.
