# Meta Geo Stalking -   &#9733; &#9733;

This OSINT challenge requires we extrapolate some metadata from the image itself. In this case we can use `exiftool`. As we extract the GPS coordinates, we can search and see what comes up. Might be around the Daniel Boone National Forest tho.
